/*
 * Check whether a **pattern** matches a **root**
 *
*/
bool rx_does_match (GraphNode root, GraphNode pattern)
{
    return false;
}

/*
 * Check whether a **pattern** matches a **root**,
 *   and return the match object.
 *
*/
PatternMatch rx_get_match (GraphNode root, GraphNode pattern)
{
    return NULL;
}